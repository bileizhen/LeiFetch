<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <files-path name="downloads" path="downloads/" />
    <!-- 诊断包写在 cacheDir/logs，分享日志时经 FileProvider 授权读取。 -->
    <cache-path name="logs" path="logs/" />
</paths>
