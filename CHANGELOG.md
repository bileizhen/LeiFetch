# 更新日志

本项目的所有重要变更都会记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- **代理支持**：设置页新增「网络 · 代理」，下载、GitHub 镜像测速与 Firefox 插件探测共用同一份配置
- 四种代理模式：不使用代理、系统代理（默认）、手动配置（HTTP / SOCKS5，支持账号密码与直连名单）、自动（推荐）；默认值与引入代理前的行为一致
- 自动模式优先跟随系统代理，其次探测本机常见代理端口（先判端口是否在监听，再按 HTTP CONNECT / SOCKS5 握手确认），都不可用时直连；结果缓存 5 分钟，探测只做本机 TCP 连接与协议握手
- 代理页提供线路连通测试，用下载内核同一套探测请求报告实际走的线路与首字节延迟
- Firefox 插件在浏览器进程内经 hook 偏好读取代理配置，独立探测与下载使用同一线路
- **运行日志**：设置 → 诊断 → 日志实时查看运行日志，新记录自动贴底跟随，往回翻暂停跟随并可一键回到最新
- 日志覆盖下载、内核、镜像、代理与捕获路径；插件进程（Firefox、系统下载器）的日志经捕获提供者回传，与应用进程并入同一条时间线
- 支持按级别（信息 / 警告 / 错误）、来源与关键字筛选，来源筛选项按实际出现过的来源生成
- 一键导出诊断包（`logs.txt` / `summary.json` / `logcat.txt`）并可保存或分享，形态沿用 XBlocker 的日志上报
- 日志只保留最近 600 条且仅存于内存；地址只记录主机名，查询串不写入日志与诊断包

## [1.0.0] - 2026-09-14

首个正式版本。相对 `0.1.0-beta.2`，本次发布主要完成了 NSFX 内核的功能性迁移与稳定性收敛，
并确认全部单元测试通过、release 构建无错误。

### 新增

- **NSFX 多线程内核**：单任务最多 16 线程，动态尾段拆分、断点恢复、指数退避、主机并发降级与全局连接 / 速度预算
- **Last-Modified 安全分段**：合法的 `Last-Modified` 可作为分段与断点续传依据，强 `ETag` 仍优先，并兼容旧版仅保存 `ETag` 的断点日志
- **文件大小提示补全**：捕获 Firefox、WebView、OkHttp、HttpURLConnection 提供的文件大小；重复捕获任务时用后续获得的大小补全已有任务
- **小文件直连**：已知小于 8 MiB 的文件直接单连接下载，省去一次 `Range: bytes=0-0` 探测；大小提示失效时自动清理临时数据并回退到完整探测流程
- **连接复用**：完整读取响应后保留 HTTP 连接复用，异常、中断或未读完时仍主动断开
- **GitHub 镜像加速**：自动识别 releases / archive / raw 等 GitHub 直链，内置 5 个镜像站，每次下载用下载地址本身并行实测，仅接受返回 206 且文件总长与直连一致的镜像，再取最快者
- **下载接管**：通用 DownloadManager / OkHttp / HttpURLConnection / WebView 捕获，Firefox GeckoView 适配，以及在 `com.android.providers.downloads` 内集中捕获入队的系统下载器插件
- **Via 第三方下载器契约**：按 ACTION_SEND + text/plain 提供导出接收入口，任意应用分享的链接可转交 LeiFetch 立即下载，无需 Hook
- **传输工作台**：实时速度曲线与任务统计，按进行中 / 已停止 / 已完成 / 全部分组并支持搜索，按当前筛选批量开始与暂停
- **任务列表手势**：右滑多选（全选 / 批量删除）、左滑删除（连同已保存文件），面板跟手与弹簧回弹
- **任务详情三视图**：信息卡（传输 / 进度 / 连接 / 常规）、Motrix 式分段点阵、速度曲线（会话 60 秒 / 生命周期切换）
- **实时卡片**：待确认、下载进度与完成经 Android 16 原生实时通知接口呈现，仅实际传输使用连续进度条
- **界面与动效**：Miuix 界面，支持浅色、深色、Monet、模糊、液态玻璃、预测性返回与全局缩放；窗口宽度达到 840 dp 时由底部导航切换为侧栏
- **双入口与作用域申请**：legacy Xposed 93 与 libxposed API 101 双入口，开启插件或接管总开关时自动向 LSPosed 申请所需作用域
- **设置项**：自定义全局限速，线程数使用弹性滑块

### 变更

- 合并 NeoNSF 的功能性迁移，保留 LeiFetch 原有的动态尾部分片、全局连接预算、限速、重定向安全与原子检查点机制
- 应用图标更换为新的 LF 标识，并改用自适应图标
- 关于页更新开发组与贡献者名单，补充 NSFX 内核贡献说明与第三方组件、隐私说明

### 修复

- 修复 CDN（如腾讯 cdntips）每次跳转更换签名 URL 时被误判为资源变化、导致断点无法跨会话续传的问题
- 服务器拒绝 Range 请求时回退单连接，不再因分段失败中断任务
- 修复剪贴板链接去重状态在应用重启后丢失的问题
- 修复 GitHub 镜像在探测阶段未做校验、可能采纳错误页或旧文件的问题

## [0.1.0-beta.2] - 2026-09-11

预发布版本，未公开发布。

### 新增

- 关于页团队与贡献者名单，展示成员贡献详情
- 打开应用时提示下载剪贴板中的链接
- GitHub 镜像设置页与应用内集成
- 传输遥测、任务详情三视图与列表入场动画

### 变更

- 关于页成员对话框与名单分组重构

### 修复

- 修复关于页成员详情对话框忽略关闭操作的问题

## [0.1.0-beta.1] - 2026-09-10

首个可构建版本，未公开发布。

### 新增

- 初始提交：Kotlin / Compose 实现的 NSFX 多线程下载管理器，含下载接管能力
- GitHub 镜像加速内核
- 系统下载器接管插件
- libxposed API 101 双入口与作用域申请
- release 签名配置

[未发布]: https://github.com/bileizhen/LeiFetch/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/bileizhen/LeiFetch/releases/tag/v1.0.0
